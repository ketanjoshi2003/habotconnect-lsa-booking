# Flask
SECRET_KEY=change-this-in-production

# Database (SQLite for dev; switch to PostgreSQL URI for production)
DATABASE_URL=sqlite:///habotconnect.db

# Mock Payment Gateway URL
# In production, replace with your real gateway (e.g. Stripe, Razorpay)
PAYMENT_GATEWAY_URL=http://localhost:5000/api/v1/payments/mock-gateway/
PAYMENT_GATEWAY_API_KEY=test-mock-key-12345
